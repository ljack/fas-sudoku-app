# Feature: leaderboard

Requirements and design spec for the leaderboard feature.

## DB Schema Setup
*   Registers schema setup in the `onBoot` hook.

## Route Enpoints
*   Exposes endpoints via `context.router`.
